<ul id='menu'>
	<li><%= link_to 'Albums', albums_path, class: 'btn btn-primary' %></li>
	<% if user_signed_in? %>
		<li><%= link_to "sign out", destroy_user_session_path, :method => :delete, class: 'btn btn-default' %></li>
	<% else %>
		<li><%= link_to "log in" , new_session_path(:user), class: 'btn btn-default' %></li>
		<li><%= link_to "register", new_registration_path(:user), class: 'btn btn-default' %></li>
	<% end %>
</ul>